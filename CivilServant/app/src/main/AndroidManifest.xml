<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a140.civilservant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ExamActivity"
            android:label="@string/activity_exam"/>
        <activity android:name=".ui.YanyuActivity"
            android:label="@string/activity_yanyu"/>
        <activity android:name=".ui.ShulActivity"
            android:label="@string/activity_shul"/>
        <activity android:name=".ui.PandActivity"
            android:label="@string/activity_pand"/>
        <activity android:name=".ui.ZiliaoActivity"
            android:label="@string/activity_ziliao"/>
        <activity android:name=".ui.ChangsActivity"
            android:label="@string/activity_changshi"/>
    </application>

</manifest>